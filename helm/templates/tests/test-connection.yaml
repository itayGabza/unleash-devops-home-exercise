apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homeAssignment.fullname" . }}-test-connection"
  labels:
    {{- include "homeAssignment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homeAssignment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
